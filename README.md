## Usage

```shell
❯ cargo run --release -q -- -h
Usage: chunks [OPTIONS] <ITEMS> <EXPECTED_ERRORS>

Arguments:
  <ITEMS>
  <EXPECTED_ERRORS>

Options:
      --mc-samples <MC_SAMPLES>
  -h, --help                     Print help
```
